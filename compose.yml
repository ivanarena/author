services:
  notes:
    build: .
    image: author-notes:local
    init: true
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - '${BIND_ADDRESS:-127.0.0.1}:${PORT:-3000}:3000'
    volumes:
      - author-notes-data:/data

volumes:
  author-notes-data:
