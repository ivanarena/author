# Sync Spec Package

This package contains executable helpers for the v1 sync protocol:

- record diffing
- conflict detection
- conflict choice selection
- 90-day retention cutoff calculation

The prose protocol lives in `docs/sync-protocol.md` and `docs/conflict-handling.md`.
